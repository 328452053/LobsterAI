diff --git a/src/agents/run-safety-controller.test.ts b/src/agents/run-safety-controller.test.ts
index 02e653d57..b182e0348 100644
--- a/src/agents/run-safety-controller.test.ts
+++ b/src/agents/run-safety-controller.test.ts
@@ -451,4 +451,27 @@ describe("RunSafetyRootCoordinator", () => {
     expect(coordinator.publishTerminalOnce(second)).toBe(false);
     expect(coordinator.publishedTerminal).toBe(first);
   });
+
+  it("releases a failed owner publication task so the retained owner can retry", async () => {
+    const reason = {
+      kind: RunSafetyTerminationKind.RunProviderDispatchBudget,
+      rootInvocationId: "root-owner-retry",
+      budgetScopeId: "scope-owner-retry",
+      runId: "run-owner-retry",
+    };
+    let deliveryAttempts = 0;
+    const coordinator = new RunSafetyRootCoordinator("root-owner-retry", async () => {
+      deliveryAttempts += 1;
+      if (deliveryAttempts <= 3) {
+        throw new Error("queued delivery failed");
+      }
+    });
+
+    await expect(coordinator.publishTerminalToOwnerOnce(reason)).resolves.toBe(false);
+    expect(deliveryAttempts).toBe(3);
+
+    await expect(coordinator.publishTerminalToOwnerOnce(reason)).resolves.toBe(true);
+    expect(deliveryAttempts).toBe(4);
+    expect(coordinator.publishedTerminal).toBe(reason);
+  });
 });
diff --git a/src/agents/run-safety-controller.ts b/src/agents/run-safety-controller.ts
index 52fc5e00b..882aa06cd 100644
--- a/src/agents/run-safety-controller.ts
+++ b/src/agents/run-safety-controller.ts
@@ -355,7 +355,7 @@ export class RunSafetyRootCoordinator {
       return this.ownerPublishTask;
     }
     const published = this.published ?? reason;
-    this.ownerPublishTask = (async () => {
+    const publishTask = (async () => {
       if (!this.onOwnerTerminal) {
         this.ownerPublishSucceeded = true;
         return true;
@@ -374,7 +374,13 @@ export class RunSafetyRootCoordinator {
       }
       return false;
     })();
-    return this.ownerPublishTask;
+    this.ownerPublishTask = publishTask;
+    return publishTask.then((succeeded) => {
+      if (!succeeded && this.ownerPublishTask === publishTask) {
+        this.ownerPublishTask = undefined;
+      }
+      return succeeded;
+    });
   }
 
   get publishedTerminal(): RunSafetyTermination | undefined {
diff --git a/src/agents/run-safety-delivery.test.ts b/src/agents/run-safety-delivery.test.ts
index 0ac291fea..05248d7bd 100644
--- a/src/agents/run-safety-delivery.test.ts
+++ b/src/agents/run-safety-delivery.test.ts
@@ -1,8 +1,14 @@
 import { afterEach, describe, expect, test } from "vitest";
-import { RunSafetyTerminationKind, type RunSafetyTermination } from "./run-safety-controller.js";
+import { copyReplyPayloadMetadata } from "../auto-reply/reply-payload.js";
+import {
+  RunSafetyRootCoordinator,
+  RunSafetyTerminationKind,
+  type RunSafetyTermination,
+} from "./run-safety-controller.js";
 import {
   buildRunSafetyDeliveryKey,
   buildRunSafetyNativeReplyPayloads,
+  buildRunSafetyNativeReplyPayloadsForOwner,
   buildRunSafetyReplyPayload,
   hasRunSafetyDeliveryReceipt,
   recordRunSafetyDeliveryReceipt,
@@ -10,6 +16,7 @@ import {
   resetRunSafetyDeliveryReceiptsForTests,
   resolveRunSafetyDeliveryLocale,
   RunSafetyDeliveryLocale,
+  setRunSafetyDeliveryClaimTimeoutMsForTests,
   waitForRunSafetyDeliveryReceipt,
 } from "./run-safety-delivery.js";
 
@@ -170,8 +177,96 @@ describe("run safety native delivery", () => {
     expect(hasRunSafetyDeliveryReceipt(reason)).toBe(true);
   });
 
+  test("retries a queued owner reply until an actual delivery receipt succeeds", async () => {
+    const reason = termination(RunSafetyTerminationKind.RunProviderDispatchBudget);
+    const deliveryResults = [{ ok: false }, { delivered: false }, { delivered: true }];
+    const deliveredPayloads: Array<ReturnType<typeof buildRunSafetyReplyPayload>> = [];
+    const coordinator = new RunSafetyRootCoordinator(reason.rootInvocationId, async (published) => {
+      const [payload] = await buildRunSafetyNativeReplyPayloadsForOwner({
+        termination: published,
+        sourceText: "continue",
+      });
+      if (!payload) {
+        return;
+      }
+      deliveredPayloads.push(payload);
+      recordRunSafetyDeliveryReceiptForPayload(payload, deliveryResults.shift());
+      if (!(await waitForRunSafetyDeliveryReceipt(published))) {
+        throw new Error("queued delivery did not produce a successful receipt");
+      }
+    });
+
+    await expect(coordinator.publishTerminalToOwnerOnce(reason)).resolves.toBe(true);
+    expect(deliveredPayloads).toHaveLength(3);
+    expect(hasRunSafetyDeliveryReceipt(reason)).toBe(true);
+    expect(
+      buildRunSafetyNativeReplyPayloads({
+        termination: { ...reason, runId: "late-child" },
+      }),
+    ).toEqual([]);
+  });
+
   test("preserves ordinary provider payloads when there is no safety terminal", () => {
     const providerPayloads = [{ text: "normal" }, { text: "second" }];
     expect(buildRunSafetyNativeReplyPayloads({ providerPayloads })).toEqual(providerPayloads);
   });
+
+  test("expires an orphaned claim so the retained owner can re-claim and deliver", async () => {
+    setRunSafetyDeliveryClaimTimeoutMsForTests(25);
+    const reason = termination(RunSafetyTerminationKind.RunToolBudget);
+    const [payload] = buildRunSafetyNativeReplyPayloads({
+      termination: reason,
+      sourceText: "continue",
+    });
+    expect(payload).toBeTruthy();
+
+    // A delivery transform that clones the payload without copying its
+    // metadata loses the receipt identity: the send outcome can no longer
+    // settle the claim, which previously left the waiter pending forever.
+    const detached = { ...payload! };
+    expect(recordRunSafetyDeliveryReceiptForPayload(detached, { ok: true })).toBe(false);
+
+    const waiter = waitForRunSafetyDeliveryReceipt(reason);
+    await expect(waiter).resolves.toBe(false);
+    expect(hasRunSafetyDeliveryReceipt(reason)).toBe(false);
+
+    const retry = await buildRunSafetyNativeReplyPayloadsForOwner({
+      termination: { ...reason, runId: "retry-owner" },
+      sourceText: "continue",
+    });
+    expect(retry).toHaveLength(1);
+    expect(recordRunSafetyDeliveryReceiptForPayload(retry[0]!, { ok: true })).toBe(true);
+    expect(hasRunSafetyDeliveryReceipt(reason)).toBe(true);
+  });
+
+  test("waits out a live claim instead of expiring it early", async () => {
+    setRunSafetyDeliveryClaimTimeoutMsForTests(60_000);
+    const reason = termination(RunSafetyTerminationKind.RunPromptExposureBudget);
+    const [payload] = buildRunSafetyNativeReplyPayloads({
+      termination: reason,
+      sourceText: "continue",
+    });
+    expect(payload).toBeTruthy();
+    expect(
+      buildRunSafetyNativeReplyPayloads({
+        termination: { ...reason, runId: "late-child" },
+      }),
+    ).toEqual([]);
+    expect(recordRunSafetyDeliveryReceiptForPayload(payload!, { ok: true })).toBe(true);
+    await expect(waitForRunSafetyDeliveryReceipt(reason)).resolves.toBe(true);
+  });
+
+  test("keeps the receipt identity on a metadata-preserving payload replacement", () => {
+    const reason = termination(RunSafetyTerminationKind.RunProviderDispatchBudget);
+    const [payload] = buildRunSafetyNativeReplyPayloads({
+      termination: reason,
+      sourceText: "continue",
+    });
+    const replaced = copyReplyPayloadMetadata(payload!, {
+      ...payload!,
+      text: `[prefix] ${payload!.text}`,
+    });
+    expect(recordRunSafetyDeliveryReceiptForPayload(replaced, { ok: true })).toBe(true);
+    expect(hasRunSafetyDeliveryReceipt(reason)).toBe(true);
+  });
 });
diff --git a/src/auto-reply/reply/agent-runner.ts b/src/auto-reply/reply/agent-runner.ts
index 33dba79f1..7fcfeafd1 100644
--- a/src/auto-reply/reply/agent-runner.ts
+++ b/src/auto-reply/reply/agent-runner.ts
@@ -19,6 +19,7 @@ import type { RunSafetyTermination } from "../../agents/run-safety-controller.js
 import {
   buildRunSafetyNativeReplyPayloads,
   buildRunSafetyNativeReplyPayloadsForOwner,
+  waitForRunSafetyDeliveryReceipt,
 } from "../../agents/run-safety-delivery.js";
 import { runSafetyRegistry } from "../../agents/run-safety-registry.js";
 import { deriveContextPromptTokens, hasNonzeroUsage, normalizeUsage } from "../../agents/usage.js";
@@ -1510,6 +1511,9 @@ export async function runReplyAgent(params: {
     });
     if (payload && opts?.onBlockReply) {
       await opts.onBlockReply(applyReplyToMode(payload));
+      if (!(await waitForRunSafetyDeliveryReceipt(reason))) {
+        throw new Error("Run-safety terminal reply was queued but not delivered.");
+      }
     }
   };
   const loopDetectionConfig = cfg.tools?.loopDetection;
@@ -1754,7 +1758,7 @@ export async function runReplyAgent(params: {
     }
 
     const runSafetyTermination = runResult.meta?.terminationReason;
-    const payloadArray = buildRunSafetyNativeReplyPayloads({
+    const payloadArray = await buildRunSafetyNativeReplyPayloadsForOwner({
       termination: runSafetyTermination,
       providerPayloads: runResult.payloads,
       sourceText:
