diff --git a/src/agents/embedded-agent-runner/stream-resolution.ts b/src/agents/embedded-agent-runner/stream-resolution.ts
--- a/src/agents/embedded-agent-runner/stream-resolution.ts
+++ b/src/agents/embedded-agent-runner/stream-resolution.ts
@@ -1,6 +1,10 @@
 import { getApiProvider } from "../../llm/api-registry.js";
 import { streamSimple } from "../../llm/stream.js";
 import { createAnthropicVertexStreamFnForModel } from "../anthropic-vertex-stream.js";
+import {
+  ProviderStreamPurpose,
+  type ProviderStreamPurpose as ProviderStreamPurposeType,
+} from "../provider-run-safety-contract.js";
 import { createBoundaryAwareStreamFnForModel } from "../provider-transport-stream.js";
 import type { StreamFn } from "../runtime/index.js";
 import { stripSystemPromptCacheBoundary } from "../system-prompt-cache-boundary.js";
@@ -122,6 +126,7 @@ export function resolveEmbeddedAgentStreamFn(params: {
   resolvedApiKey?: string;
   authProfileId?: string;
   authStorage?: { getApiKey(provider: string): Promise<string | undefined> };
+  purpose?: ProviderStreamPurposeType;
 }): StreamFn {
   if (params.providerStreamFn) {
     return wrapEmbeddedAgentStreamFn(params.providerStreamFn, {
@@ -144,2 +149,3 @@ export function resolveEmbeddedAgentStreamFn(params: {
   const currentStreamFn = params.currentStreamFn ?? streamSimple;
+  const purpose = params.purpose ?? ProviderStreamPurpose.Agent;
   if (params.model.provider === "anthropic-vertex") {
@@ -172,5 +178,8 @@ export function resolveEmbeddedAgentStreamFn(params: {
-  if (
-    isDefaultOpenClawStreamFnForModel(params.model, params.currentStreamFn) ||
-    hasResolvedRuntimeApiKey(params.resolvedApiKey)
-  ) {
+  // Utility callers such as /btw do not establish the host-owned Agent
+  // run-safety scope required by boundary-aware streams.
+  const shouldUseAgentBoundary =
+    purpose === ProviderStreamPurpose.Agent &&
+    (isDefaultOpenClawStreamFnForModel(params.model, params.currentStreamFn) ||
+      hasResolvedRuntimeApiKey(params.resolvedApiKey));
+  if (shouldUseAgentBoundary) {
     const boundaryAwareStreamFn = createBoundaryAwareStreamFnForModel(params.model);
diff --git a/src/agents/embedded-agent-runner/stream-resolution.test.ts b/src/agents/embedded-agent-runner/stream-resolution.test.ts
--- a/src/agents/embedded-agent-runner/stream-resolution.test.ts
+++ b/src/agents/embedded-agent-runner/stream-resolution.test.ts
@@ -4,2 +4,3 @@ import { getApiProvider } from "../../llm/api-registry.js";
 import { streamSimple } from "../../llm/stream.js";
+import { ProviderStreamPurpose } from "../provider-run-safety-contract.js";
 import * as providerTransportStream from "../provider-transport-stream.js";
@@ -150,1 +151,32 @@ describe("resolveEmbeddedAgentStreamFn", () => {
+  it("keeps default Agent fallbacks on boundary transports with runtime auth", () => {
+    const streamFn = resolveEmbeddedAgentStreamFn({
+      currentStreamFn: streamSimple,
+      sessionId: "session-1",
+      model: {
+        api: "openai-completions",
+        provider: "qwen",
+        id: "qwen3.6-plus",
+      } as never,
+      resolvedApiKey: "runtime-key",
+    });
+
+    expect(streamFn).not.toBe(streamSimple);
+  });
+
+  it("keeps utility streamSimple fallbacks outside Agent boundary transports", () => {
+    const streamFn = resolveEmbeddedAgentStreamFn({
+      currentStreamFn: streamSimple,
+      sessionId: "session-1",
+      model: {
+        api: "openai-completions",
+        provider: "qwen",
+        id: "qwen3.6-plus",
+      } as never,
+      resolvedApiKey: "runtime-key",
+      purpose: ProviderStreamPurpose.Utility,
+    });
+
+    expect(streamFn).toBe(streamSimple);
+  });
+
   it("routes Codex responses fallbacks through OpenClaw native transport", async () => {
diff --git a/src/agents/btw.ts b/src/agents/btw.ts
--- a/src/agents/btw.ts
+++ b/src/agents/btw.ts
@@ -512,2 +512,3 @@ export async function runBtwSideQuestion(
     authProfileId: resolvedAuthProfileId,
+    purpose: ProviderStreamPurpose.Utility,
   });
diff --git a/src/agents/btw.test.ts b/src/agents/btw.test.ts
--- a/src/agents/btw.test.ts
+++ b/src/agents/btw.test.ts
@@ -962,6 +962,7 @@ describe("runBtwSideQuestion", () => {
         sessionId: "session-1",
         resolvedApiKey: "secret",
         authProfileId: "profile-1",
+        purpose: registerProviderStreamForModelMock.mock.calls[0]?.[0]?.purpose,
       }),
     );
     expect(streamSimpleMock).toHaveBeenCalledTimes(1);
